apiVersion: apps/v1
kind: Deployment
metadata:
  name: golang-app
spec:
  revisionHistoryLimit: 1
  replicas: 4
  selector:
    matchLabels:
      app: golang-app
  template:
    metadata:
      labels:
        app: golang-app
    spec:
      containers:
      - name: golang-app
        image: therealbatman1/golang-devops-app:v5
        ports:
        - containerPort: 8080
